# FASH Account Dropdown Fix

The navbar account button opens the dropdown. The account/name header and My Account item now navigate to `pages/account.html` (or `account.html` when already inside the pages folder).
